export interface BudgetItem {
  id: string
  title: string
  amount: number
  category: string
  date: string
}

export interface TransactionItem {
  id: string
  description: string
  amount: number
  category: string
  date: string
}